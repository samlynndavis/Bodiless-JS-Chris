import { asTokenGroup } from '../../../util';

const meta = {
  categories: {
    Type: ['Element'],
    Group: ['Fonts'],
  },
};

export const cxElementFonts = asTokenGroup(meta)({
  DMSans: 'font-DMSans',
});
