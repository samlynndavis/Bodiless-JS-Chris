import { vitalFontSizeSpec } from '@bodiless/vital-elements';
import { createTokenStories } from '@bodiless/tokens';

const { meta, story } = createTokenStories(vitalFontSizeSpec);

export default meta;
export const Clean = story();
